node_modules/
.next/
out/
next-env.d.ts
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo

# fallow local cache / impact state (never committed)
.fallow/

# Claude Code: track shared config/hooks/skills, never personal or runtime files
.claude/settings.local.json
.claude/*.lock

# Coverage scratch (Batch 0057): raw V8 + intermediate Istanbul output from
# regenerating the gate snapshot. The committed snapshot the fallow gate reads
# lives at coverage/provider/coverage-final.json (tracked, NOT ignored).
.coverage-tmp/
