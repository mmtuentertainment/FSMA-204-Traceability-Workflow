# Shell scripts must use LF so they execute on Linux/WSL (e.g. the fallow commit-gate hook
# .claude/hooks/fallow-gate.sh). Without this, a CRLF checkout fails with `bad interpreter: ...^M`.
*.sh text eol=lf
